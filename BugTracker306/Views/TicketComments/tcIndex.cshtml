@model IEnumerable<BugTracker306.Models.TicketComment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ticket.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.DisplayName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Verbiage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreateDt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ticket.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.DisplayName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Verbiage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreateDt)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
